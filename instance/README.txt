The application creates fraud_detection.db in this folder automatically.

The database contains only application users and their prediction logs.
It is safe to remove the database during development if you want a fresh start.
Do not delete a production database without first making a backup.
